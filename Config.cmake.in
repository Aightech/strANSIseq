@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Replace <LIB_NAME> with your actual library name
include("${CMAKE_CURRENT_LIST_DIR}/<LIB_NAME>Targets.cmake")

# Optionally provide a version variable for external checks
set(<LIB_NAME>_VERSION "@PACKAGE_VERSION@")
